<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude WebView cache and local storage from backups -->
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="cache" path="." />
    <exclude domain="external" path="." />
</full-backup-content>
