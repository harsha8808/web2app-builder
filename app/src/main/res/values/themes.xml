<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme — no action bar -->
    <style name="Theme.Web2App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
    </style>

    <!-- Splash theme — full screen, solid color background, no flicker -->
    <style name="Theme.Web2App.Splash" parent="Theme.Web2App">
        <item name="android:windowBackground">@color/colorSplashBackground</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/colorSplashBackground</item>
        <item name="android:navigationBarColor">@color/colorSplashBackground</item>
    </style>

</resources>
